"""
Data providers package.

All provider modules in this package are auto-discovered by the registry.
To add a new provider, simply create a new .py file here that defines a
class inheriting from BaseProvider with a non-empty `name` attribute.
"""
